[project]
name = "se"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.3",
    "selenium>=4.44.0",
    "webdriver-manager>=4.1.1",
]
